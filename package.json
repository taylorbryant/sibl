{
  "name": "sibl-workspace",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "bun run build:package && bun run build:docs",
    "build:docs": "bun run --cwd docs build",
    "build:package": "bun run --cwd packages/sibl build",
    "dev": "bun run build:package && bun run --cwd docs dev",
    "test": "bun test",
    "typecheck": "bun run build:package && bun run --cwd packages/sibl typecheck && bun run --cwd docs typecheck",
    "check": "bun run typecheck && bun run test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.1.1",
    "typescript": "6.0.3"
  }
}
