---
"@sibl/docs": minor
---

Add typed GitHub, npm, PyPI, and external project links with built-in accessible presentation, plus an optional sidebar version badge. Link entries now require an explicit `type`.
